{
  "name": "saud-im-website",
  "version": "1.0.0",
  "private": true,
  "description": "saud.im - public static website of Saud Apps. Node dependencies exist only for the release-data sync workflow, not for building or serving the site.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "jsonwebtoken": "9.0.3"
  }
}
